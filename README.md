# PublicSafetyProject
# Install Expo CLI
npm install -g expo-cli

# Create project
expo init SmartSafetyDemo
cd SmartSafetyDemo

# Install dependencies
expo install react-native-maps expo-image-picker react-native-url-polyfill
npm install @supabase/supabase-js react-native-exif


- cd SmartSafetyDemo
- npm start # you can open iOS, Android, or web from here, or run them directly with the commands below.
- npm run android
- npm run ios
- npm run web

npx expo start --tunnel


dependencies:
expo install react-native-maps expo-image-picker react-native-url-polyfill
npm install @supabase/supabase-js react-native-exif
npm install react-native-maps-clustering
npm install react-native-maps react-native-maps-clustering

expo install expo-media-library expo-image-picker
expo install expo-location